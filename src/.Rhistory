library(tidyverse)
orders <- read_csv("../../data/orders.csv")
dim(orders)
names(orders)
orders %>% unique(user_id)
orders %>% unique(user_id)
orders %>% nlevels(user_id)
nlevels(orders$user_id)
nlevels(as.factor(orders$user_id))
q()
q()
